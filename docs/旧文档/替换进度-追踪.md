# AntClaw 100% 替代 ARK · 实施进度追踪

> 本文档持续更新，作为多会话施工的状态枢纽。每个 checkpoint 完成后必须刷新此文件。

## 完成进度总览（2026-04-29 02:18 UTC 更新）

| 阶段 | 状态 | E2E 验收 |
|---|---|---|
| 阶段 0 凭据核对 + 双份 client 清理 | ✅ | 9/9 vendor api_key + zhipu provider 全部已配；9 个旧平铺 client 已删；CFTC 抽到 `apiclient/cftc/` |
| 阶段 1 M1 协议合规 | ✅ | grep 探针全 PASS；onchain 465 条；COT 312 条；CFTC 子包 |
| 阶段 2 M2 期权与 Walk-Forward | 🟡 后端 ✅ / 前端 ❌ | SC-3/4/11/12/13 真数据 PASS；vol 服务已替换 40+ randFloat → 真 Deribit/CBOE；但 `features/options` 等前端模块未落地 |
| 阶段 3 M3 宏观全谱 | 🟡 后端 ✅ / 前端 ❌ | SC-8/9/10 真数据 PASS；BIS/IMF/WB/ECB/Eurostat/OECD/SNB/DTCC/Treasury/FedWatch 子包均接通 |
| 阶段 4 M4 链上/DeFi/SEC/情绪 | 🟡 后端 ✅ / 前端 ❌ | SC-5/6/7/14 真数据 PASS；Insider/CryptoSocial/MOVE 仍待真端点接入 |
| 阶段 5 M5 AI 编排 | 🟡 代码 ✅ / 凭据余额 ❌ | Chat/Interpret/Outlook 全链路代码就绪；`ai_cache` 表已建；zhipu 1113 余额不足导致 SC-15/16 fail |
| 阶段 6 M6 零回退验收 | ✅ 后端部分 | `scripts/e2e/run_all.sh` 18 SC 场景；16/18 PASS（88.9%）；`docs/AntClaw-100替代验收报告.md` 已生成 |
| 13 个前端 features 模块 | ❌ | 不在已完成范围内（需独立前端会话） |

## 本会话累计实施清单

### 新增
- `backend/internal/infra/apiclient/cftc/client.go`（CFTC 薄客户端）
- `backend/internal/service/strategy/baseline_runner.go`（替代 mock_runner）
- `backend/internal/service/vol/service.go`（**完整重写**，从 477 行 mock 改为 ~470 行真数据，0 处 randFloat）
- `scripts/e2e/_lib.sh`、`scripts/e2e/run_all.sh`、`scripts/e2e/sc-01.sh` ~ `sc-18.sh`（19 个 E2E 文件）
- `docs/凭据就绪核对.md`、`docs/替换进度-追踪.md`、`docs/AntClaw-100替代验收报告.md`

### 修复
- `backend/internal/infra/apiclient/ustreasury/client.go`：XML feed 已下线 → 重写为 CSV 解析
- `backend/internal/infra/apiclient/worldbank/client.go`：URL 缺 `/indicator/`
- `backend/internal/infra/apiclient/firecrawl/client.go`：增 `NewClientWithKey` / `SetAPIKey`，从数据库 resolver 注入凭据
- `backend/internal/infra/apiclient/cboe/client.go`：新增 `FetchVIXHistory`（CBOE 历史 JSON）
- `backend/internal/infra/apiclient/deribit/client.go`：新增 `GetVolatilityIndexData`（DVOL）
- `backend/internal/adapter/rpc/fedwatch_handler.go`、`sentiment_extras_handler.go`、`ai_handler.go`：resolver/Chat 路径
- `backend/internal/service/ai/service.go`：新增 `ChatOnce` / `chatCompletion`；URL 兼容 zhipu paas/v4
- `backend/cmd/antclaw-worker/collector_onchain.go`：长表 schema 重写，记录入库
- `backend/cmd/antclaw-worker/collector_cot.go`：迁到 `cftc` 子包
- `backend/internal/service/cot/cot_service.go`：迁到 `cftc` 子包
- `backend/internal/service/macro/macro_service.go`：移除死代码 BIS/FedWatch 字段
- `backend/cmd/antclaw-api/main.go`：resolver 路径 + BaselineRunner

### 删除（9 个旧平铺 client）
`bis_client.go`、`onchain_client.go`、`sec_client.go`、`treasury_client.go`、`worldbank_client.go`、`vix_client.go`、`fedwatch_client.go`、`sentiment_client.go`、`cftc_client.go` 及 `service/strategy/mock_runner.go`

### 数据库变更
- 新建 `ai_cache(fingerprint, operation, model, result, created_at, expires_at)`
- 应用 migration `011_create_price_tables.sql`（`price_daily` / `price_intraday`）
- `system_ai_configs.zhipu.primary_for` 加 "analysis"

## E2E 当前结果

```
sc-01 PASS   sc-02 PASS   sc-03 PASS   sc-04 PASS   sc-05 PASS
sc-06 PASS   sc-07 PASS   sc-08 PASS   sc-09 PASS   sc-10 PASS
sc-11 PASS   sc-12 PASS   sc-13 PASS   sc-14 PASS   sc-15 FAIL*
sc-16 FAIL*  sc-17 PASS   sc-18 PASS
* zhipu 余额不足，非代码缺陷
```

## 下次施工聚焦

1. 前端 13 个 features 模块（按 `docs/ARK替代实施方案/10-前端模块规范.md`）
2. `service/sentiment` / `service/ta` / `service/price` 中余下 `randFloat() = 0.5` 兜底替换为 DB 查询（依赖 `sentiment_snapshots` 等 schema 迁移先行）
3. `Insider / CryptoSocial / MOVE` 通过 firecrawl 接通
4. `fred_client.go` / MQL5 fetcher 迁到 `apiclient/fred/`、`apiclient/mql5/` 子包
5. AI provider 余额充值 → 重跑 SC-15/16 应直接 PASS

## 复查命令

```bash
cd /opt/antclaw
docker compose -f deploy/docker-compose.yaml ps
bash scripts/e2e/run_all.sh
docker exec antclaw-postgres psql -U antclaw -d antclaw \
  -c "SELECT 'price_daily' t, count(*) FROM price_daily UNION ALL \
      SELECT 'cot_records', count(*) FROM cot_records UNION ALL \
      SELECT 'onchain_metrics', count(*) FROM onchain_metrics;"
```
