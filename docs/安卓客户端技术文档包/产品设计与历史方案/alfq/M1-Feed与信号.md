# AlfQ · M1 · Feed + 信号

> 交易员版 X，信号驱动的社交网络。后端复用 AntClaw，4 Tab（Feed / 发现 / 发布 / 我）。
> 完整上下文见 [总索引](../AlfQ-安卓客户端设计文档.md)。

## 线框

### Feed 页

```
┌──────────────────────────────────┐
│ [🔍 搜索交易员/品种...]  [筛选▼] │
├──────────────────────────────────┤
│  EURUSD↗85% GBPUSD↗78% USDJPY↘62%│  ← 横向滚动信号条
│  [▶ 展开信号详情]                │
├──────────────────────────────────┤
│ ● 系统信号                  5m前 │
│ ┌──────────────────────────────┐ │
│ │ EURUSD ↗ 看多 置信度 85%    │ │
│ │ COT偏多 · TA强势 · Sentiment│ │
│ │ [订阅信号] [查看详情]        │ │
│ └──────────────────────────────┘ │
│                                  │
│ ● 系统警报               12m前   │
│ ⚠ VIX 突破 25，波动加剧         │
│ [查看详情] [静默此类]           │
└──────────────────────────────────┘
```

> **下拉刷新**：Feed 顶部下拉触发增量拉取。标准 Material 3 `pullToRefresh`。

### 信号详情页（从信号条/信号卡片点入）

```
┌──────────────────────────────────┐
│ ← EURUSD 信号详情        [分享]  │
├──────────────────────────────────┤
│   1.0850    ↗ 看多   置信度 85%  │
│   ┌─ 日线 K 线图 ──────────────┐ │
│   │  ╱╲    ╱╲                  │ │
│   │ ╱  ╲  ╱  ╲   ╱╲           │ │
│   │╱    ╲╱    ╲╱  ╲  ...       │ │
│   └────────────────────────────┘ │
│   因子分解                       │
│   COT 0.72  TA 0.88  Sent 0.55  │
│   [订阅此品种警报] [发帖讨论]    │
└──────────────────────────────────┘
```

---

## 系统信号自动发帖规则

- 触发：信号方向变更时自动生成帖子
- 去重：同一品种同方向 **4 小时内不重复**
- 身份：`系统信号`（官方账号）

---

## 数据流

```
Feed 加载
  │
  ├─→ SignalsService.GetBias (自选品种)
  │     └→ 顶部信号条数据
  │
  ├─→ SignalsService.GetUnified (信号卡片)
  │     └→ Feed 混合流
  │
  ├─→ SSE feed:posts (实时增量)
  │
  └─→ Room 缓存 (200 条)

信号详情
  │
  ├─→ SignalsService.GetUnified (多因子)
  ├─→ PriceService.GetBars (20 根日线 K 线)
  └─→ Canvas 渲染
```

---

## 依赖
- 复用：`SignalsService` / `PriceService` / `AlertsService`（AntClaw 现有）
- **不依赖**新增后端 Service

## 验收
- [ ] Feed 页：混合信息流 + 顶部信号条
- [ ] 信号详情页：多因子分解 + K 线图
- [ ] 信号条 SSE 实时刷新
- [ ] 品种搜索 + 自选管理
- [ ] 下拉刷新可用
---

## 共享上下文

> 以下通用约定在索引文档中定义，此处仅摘要。开发时参照索引文档获取完整信息。

| 项目 | 约定 |
|---|---|
| RPC 端点 | `POST /antclaw.v1.<Service>/<Method>`，`Content-Type: application/json` |
| 认证 | `Authorization: Bearer <JWT>`，token 过期跳转登录页 |
| SSE 连接 | `GET /sse/<channel>` + `Authorization: Bearer <JWT>`，消息格式 `event: <type>
data: <json>` |
| 重连 | 指数退避 1s→2s→4s→max 30s |
| 分页 | 列表类 RPC 统一 `cursor` 分页，每页 20 条 |
| 缓存 | Room 本地缓存，新数据覆盖，无网络永久保留 |
| 错误 | `CodeUnauthenticated` → 登录页；`CodeUnavailable` → 缓存+提示；其余 → toast message |
| 版本 | 所有新 Service 注册到 `proto/antclaw/v1/`，改 proto 后执行 `buf generate` |
