# AlfQ · M5 · 市场 + 打磨

> 交易员版 X，信号驱动的社交网络。后端复用 AntClaw，4 Tab（Feed / 发现 / 发布 / 我）。
> 完整上下文见 [总索引](../AlfQ-安卓客户端设计文档.md)。

## 线框

### 交易市场

```
┌──────────────────────────────────────────┐
│  市场                    [🔍 搜索] [筛选] │
├──────────────────────────────────────────┤
│  热门  │  策略  │  指标  │  信号  │  EA  │
├──────────────────────────────────────────┤
│ ┌──────────────────────────────────────┐ │
│ │ 🔵 趋势捕手 Quant v2                │ │
│ │ @Alex Chen · 4.8★ · 1.2k 购买      │ │
│ │ EURUSD 趋势策略 · MT5 EA             │ │
│ │ 💰 $29.99/月  [试用 7 天] [购买]    │ │
│ └──────────────────────────────────────┘ │
│ ┌──────────────────────────────────────┐ │
│ │ 🟢 Gold Scalper Indicator            │ │
│ │ @LiWei · 4.6★ · 890 购买            │ │
│ │ XAUUSD 超短线 · TradingView 指标     │ │
│ │ 💰 $14.99/月  [试用 3 天] [购买]    │ │
│ └──────────────────────────────────────┘ │
└──────────────────────────────────────────┘
```

---

## Marketplace 业务规则

| 规则 | 值 |
|---|---|
| 上架资格 | 🟢 认证交易员 或 🔵 精英交易员 |
| 抽佣 | 信号 30% / 指标EA 25% / 源码 20%（后台可调） |
| 试用期 | 提供者可选（3/7/14 天） |
| 退款 | 48h 无条件，退款后自动解绑 MT |
| 购买类型 | `subscription`（月付）/ `one_time`（永久） |
| 结算 | 月结，平台抽佣后打入提供者账户 |

---

## 数据流

```
浏览市场
  ├─→ MarketplaceService.ListProducts (RPC)
  └─→ 按类别/评分筛选

购买
  ├─→ MarketplaceService.PurchaseProduct (RPC)
  ├─→ 支付处理（Stripe/平台内余额）
  └─→ 策略自动绑定到指定 MT 账号
```

---

## 依赖
- 新增后端：`MarketplaceService`（proto + 实现）
- 数据库新表：`marketplace_products`, `purchases`, `revenue_records`

## 验收
- [ ] 市场浏览 + 搜索 + 筛选
- [ ] 购买/订阅流程
- [ ] 试用期 + 自动续费/取消
- [ ] 48h 退款 + 策略解绑
- [ ] Feed 虚拟滚动（>100 条不卡）
- [ ] Sentry 崩溃收集
- [ ] Google Play 上架
---

## 共享上下文

> 以下通用约定在索引文档中定义，此处仅摘要。开发时参照索引文档获取完整信息。

| 项目 | 约定 |
|---|---|
| RPC 端点 | `POST /antclaw.v1.<Service>/<Method>`，`Content-Type: application/json` |
| 认证 | `Authorization: Bearer <JWT>`，token 过期跳转登录页 |
| SSE 连接 | `GET /sse/<channel>` + `Authorization: Bearer <JWT>`，消息格式 `event: <type>
data: <json>` |
| 重连 | 指数退避 1s→2s→4s→max 30s |
| 分页 | 列表类 RPC 统一 `cursor` 分页，每页 20 条 |
| 缓存 | Room 本地缓存，新数据覆盖，无网络永久保留 |
| 错误 | `CodeUnauthenticated` → 登录页；`CodeUnavailable` → 缓存+提示；其余 → toast message |
| 版本 | 所有新 Service 注册到 `proto/antclaw/v1/`，改 proto 后执行 `buf generate` |
