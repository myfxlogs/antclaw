# AlfQ · M3 · 社交 + 身份

> 交易员版 X，信号驱动的社交网络。后端复用 AntClaw，4 Tab（Feed / 发现 / 发布 / 我）。
> 完整上下文见 [总索引](../AlfQ-安卓客户端设计文档.md)。

## 线框

### 帖子详情页

```
┌──────────────────────────────────┐
│ ← 帖子                          │
├──────────────────────────────────┤
│ Alex Chen · 🟢          2h 前   │
│ 刚平掉 XAUUSD 多单，+2.3%       │
│ ┌──────────────────────────────┐ │
│ │ XAUUSD 平仓收益 +$1,450     │ │
│ │ 入场 2340.5 → 出场 2394.2   │ │
│ └──────────────────────────────┘ │
│ ♡ 12   💬 3                     │
├──────────────────────────────────┤
│ ● Li Wei：漂亮的入场点！  1h前  │
│ ● Zhang：止损设在哪？     30m前 │
│ ┌──────────────────────────────┐ │
│ │ [发表评论...]          [发送]│ │
│ └──────────────────────────────┘ │
└──────────────────────────────────┘
```

### 冷启动引导页

```
┌──────────────────────────┐
│   欢迎来到 AlfQ          │
│                          │
│   关注交易员，获取信号   │
│   [推荐关注]             │
│   Alex Chen · 趋势策略   │
│   Li Wei · 黄金短线     │
└──────────────────────────┘
```

---

## 数据流

```
发帖
  │
  ├─→ FeedService.CreatePost (RPC)
  │     └→ text + signal_card + visibility
  │
  └─→ SSE feed:posts → 关注者 Feed 刷新

社交交互
  ├─→ FeedService.LikePost / CommentOnPost / SharePost (RPC)
  └─→ SSE notifications → 被互动方收到通知
```

---

## 依赖
- 新增后端：`FeedService` + `TraderService`（proto + 实现）
- 数据库新表：`posts`, `comments`, `likes`, `shares`, `follows`, `trader_profiles`

## 验收
- [ ] 发帖：文本 + 引用信号 + 可见范围
- [ ] 点赞/评论/转发交互
- [ ] 个人主页：用户自选公开指标，风险提示
- [ ] 🔵 精英认证：人工审核入口
- [ ] 新用户冷启动引导页
---

## 共享上下文

> 以下通用约定在索引文档中定义，此处仅摘要。开发时参照索引文档获取完整信息。

| 项目 | 约定 |
|---|---|
| RPC 端点 | `POST /antclaw.v1.<Service>/<Method>`，`Content-Type: application/json` |
| 认证 | `Authorization: Bearer <JWT>`，token 过期跳转登录页 |
| SSE 连接 | `GET /sse/<channel>` + `Authorization: Bearer <JWT>`，消息格式 `event: <type>
data: <json>` |
| 重连 | 指数退避 1s→2s→4s→max 30s |
| 分页 | 列表类 RPC 统一 `cursor` 分页，每页 20 条 |
| 缓存 | Room 本地缓存，新数据覆盖，无网络永久保留 |
| 错误 | `CodeUnauthenticated` → 登录页；`CodeUnavailable` → 缓存+提示；其余 → toast message |
| 版本 | 所有新 Service 注册到 `proto/antclaw/v1/`，改 proto 后执行 `buf generate` |
