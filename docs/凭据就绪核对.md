# 凭据就绪核对（阶段 0 产物）

> 生成时间：2026-04-28
> 数据库：`antclaw-postgres`
> 范围：M1~M5 真实数据验收所需的 vendor API Key 与 AI Provider 凭据

## 1. data_source_configs（vendor 凭据）

| source_id | kind | has_secret | 备注 |
|---|---|---|---|
| alpha_vantage | api_key | ✅ | 行情备援 |
| bybit | api_key | ✅ | 期货行情（含 GEX/IV 公共端点） |
| cftc_socrata | api_key | ⚪ | 用户确认无需 Key（走 legacy CSV） |
| coingecko | api_key | ✅ | 加密行情/链上 |
| eia | api_key | ✅ | 能源数据 |
| firecrawl | api_key | ✅ | FedWatch / MyFXBook / Finviz / TradingEconomics 抽取依赖 |
| fred | api_key | ✅ | FRED 宏观 |
| massive | api_key | ✅ | 历史面板 |
| twelve_data | api_key | ✅ | 行情主力 |

公共端点（`kind=endpoint`，免 Key 但受速率限制）共 24 家，覆盖：
BIS、ECB、Eurostat、OECD、SNB、IMF、World Bank、US Treasury、SEC EDGAR、CBOE、CME（fed_rss）、Coinmetrics、CryptoCompare、DefiLlama、Deribit、Blockchain.info、DTCC、Stooq、Yahoo、Alternative.me、CNN Fear&Greed、TreasuryDirect、MQL5。

## 2. system_ai_configs（AI Provider 凭据）

| provider_id | enabled | has_secret | default_model |
|---|---|---|---|
| zhipu | ✅ | ✅ | glm-4.5 |
| 其他 7 家 | f | f | — |

至少一家 AI Provider 已就绪；M5 AI 编排可全量真实验收。

## 3. 待运维补齐项（M6 验收报告需要列出）

- finviz、myfxbook、openinsider、tradingeconomics、cme_fedwatch 未在 `data_source_configs` 单独种子；当前通过 firecrawl 通路抽取，运维可后续单独建子源以便监控独立健康度。

## 4. 凭据热更新机制

- 后端通过 `datasource.CredentialResolver` 缓存解密后的 Key，`/datasources` 写入会触发 Redis Pub/Sub `data_sources:onChange` → resolver `ReloadAll` → 各 vendor 客户端重建。
- Firecrawl 客户端已重构为支持 `NewClientWithKey(src, key)` 直接注入；FedWatch/SentimentExtras handler 在 main.go 用 resolver 路径构造（`NewFedWatchHandlerWithResolver` / `NewSentimentExtrasHandlerWithResolver`）。
