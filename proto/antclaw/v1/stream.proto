syntax = "proto3";

package antclaw.v1;

option go_package = "github.com/antclaw/antclaw/gen/go/antclaw/v1;antclawv1";

import "google/protobuf/struct.proto";

// StreamService provides real-time streaming via Connect server-streaming RPC.
// Replaces legacy SSE text/event-stream with protobuf binary streaming.
service StreamService {
  rpc SubscribeEvents(SubscribeEventsRequest) returns (stream SubscribeEventsResponse);
}

message SubscribeEventsRequest {
  string channel = 1;
  string last_event_id = 2;
}

message SubscribeEventsResponse {
  string id = 1;
  string type = 2;
  // Structured payload (replaces legacy JSON string)
  google.protobuf.Struct payload = 3;
  string timestamp = 4;
}

// EventChannel enum for channel routing
enum EventChannel {
  EVENT_CHANNEL_UNSPECIFIED = 0;

  EVENT_CHANNEL_PRICE = 1;
  EVENT_CHANNEL_PRICE_TICK = 2;

  EVENT_CHANNEL_ALERT = 10;
  EVENT_CHANNEL_ALERT_TRIGGERED = 11;
  EVENT_CHANNEL_NOTIFY_NEW = 12;

  EVENT_CHANNEL_SIGNAL_EMITTED = 20;

  EVENT_CHANNEL_TASK_PROGRESS = 30;
  EVENT_CHANNEL_BACKTEST = 31;

  // Admin channels
  EVENT_CHANNEL_JOBS = 40;
  EVENT_CHANNEL_AUDIT = 41;
  EVENT_CHANNEL_NOTIFICATIONS = 42;
  EVENT_CHANNEL_MACRO_ALERTS = 43;
  EVENT_CHANNEL_OPTIONS_ALERTS = 44;
  EVENT_CHANNEL_SIGNALS_ALERTS = 45;

  EVENT_CHANNEL_STREAM_KEEPALIVE = 100;
  EVENT_CHANNEL_STREAM_CLOSED = 101;
}
